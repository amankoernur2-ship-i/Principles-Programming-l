// Conditional statements - if, else, else if

#include <iostream>

using namespace std;

int main() {

    // sample, does not compiles

    if(/*condition - either a boolean value, true/false, 
        or an expression that returns a boolean value*/) {
            // body of the if block
            // this code runs if the condition is true
            // otherwise, it is skipped
    }

    return 0;
}