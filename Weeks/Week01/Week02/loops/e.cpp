
// Loops - for

#include <iostream>

using namespace std;

int main() {

    int a;
    cin >> a;

    while(a > 5) {
        if(a > 5) break;
        cout << a << endl;
        a += 1; // same as a = a + 1;
    }

    return 0;
}